## Summary

<!-- What does this PR do, in 1-3 sentences? -->

## Phase / Area

<!-- e.g. Phase 4 — Pagination, or "hotfix: click counter race condition" -->

## Changes

-
-

## Testing

<!-- How was this verified? Paste relevant command output if useful. -->

- [ ] `npm test --prefix backend` passes locally
- [ ] `npm run lint` passes locally
- [ ] Manually verified against a running instance (describe how)

## Checklist

- [ ] No secrets or `.env` values committed
- [ ] Prisma migrations included if the schema changed
- [ ] OpenAPI spec (`openapi.yaml`) updated if the API surface changed
